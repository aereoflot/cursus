from setuptools import setup, find_packages

setup(
    name="mazegen-ancrodri", # Usa tu login
    version="1.0.0",
    py_modules=["mazegen"],
    install_requires=[], # Añade dependencias si usas librerías externas
)