
[tool.poetry]
name = "matrix-operator"
version = "1.0.0"
description = "Data analysis tool"
authors = ["Ancrodri <ancorodrigezortega@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.11"
pandas = ">=2.2.0"
requests = ">=2.31.0"
matplotlib = ">=3.8.0"
numpy = ">=1.26.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"